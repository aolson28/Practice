def check(a, x):
    return x in a

# P bool
# R retrun true if a contains x and false if a does not contain x
# E a = [1, -1, 2, -2, 3, c, 'test'] b = 'test'-> True
# P the seq a and elem x were not in the function, use x in a to give the bool