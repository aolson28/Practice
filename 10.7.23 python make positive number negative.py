def make_negative( number ):
    if number > 0:
        return number * -1
    else:
        return number

# P integer
# R show the negative of a given number, ignoring currently negative numbers
# E 5 -> -5
# P use if and else. if number > 0 excludes all values not to be changed